# -------------------------------------------------------
# Required: shadows src/main/resources/application.properties
# on the test classpath. Must re-declare any property that
# is injected via @Value or required for bean initialization.
# -------------------------------------------------------
spring.application.name=discovery-server

eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false

# Credentials for the test context — UserDetailsService reads these from Environment
spring.security.user.name=eureka-test
spring.security.user.password=test-password
