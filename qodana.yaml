version: "1.0"

linter: jetbrains/qodana-jvm-community:latest

profile:
  name: qodana.recommended

exclude:
  - name: All
    paths:
      - build
      - .gradle
      - infrastructure
      - "**/*Test.java"
      - "**/*IT.java"

failThreshold: 10

projectJDK: "25"